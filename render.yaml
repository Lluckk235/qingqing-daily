services:
  - type: web
    name: qingqing-daily
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 server.py 8000
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0